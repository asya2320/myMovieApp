import { createTheme } from '@material-ui/core';

export const theme = createTheme({
    palette: {
        type: 'light',
        primary: {
            main: '#ffa500',
        },
        secondary: {
            main: '#f50057',
        },
        background: {
            default: '#999999',
        },
    },
});
